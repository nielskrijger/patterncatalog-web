var React = require('react');

module.exports = React.createClass({
    render: function() {
        return (
            <div className="error notfound">
                Page not found.
            </div>
        );
    }
});
